import * as React from 'react';
import { Button, Text } from 'react-native';

export const RoutinesScreen = ({ navigation }) => {
    return (
      <Text>This is the routines screen</Text>
      );
};